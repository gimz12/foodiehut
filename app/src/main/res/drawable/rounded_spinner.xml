<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/> <!-- Change to your preferred background color -->
            <corners android:radius="12dp"/> <!-- Rounded corners -->
            <padding
                android:left="8dp"
                android:right="8dp"
                android:top="4dp"
                android:bottom="4dp"/>
            <stroke
                android:width="1dp"
                android:color="#B0B0B0"/> <!-- Border color -->
        </shape>
    </item>

    <!-- Dropdown arrow icon -->
    <item android:right="16dp" android:gravity="end|center_vertical">
        <bitmap

        android:gravity="center_vertical"/>
    </item>

</layer-list>
